# Dev only. Reasoning (why alpine, rebuild-on-new-dep rule): docs/ARCHITECTURE.md
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0", "-p", "3000"]
